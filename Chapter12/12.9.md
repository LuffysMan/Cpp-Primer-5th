```
//下面两行代码, 第一行在堆上申请了两块内存, 分别用q和r保存内存地址; 第二行将q的值赋给r, r原本保存的地址丢失, 内存泄露;
int *q = new int(42), *r = new int(100);
r = q;

//在堆上申请两块内存, 分别用智能指针q2, r2保存其地址; 执行r2=q2后, 值为100的内存引用计数减为0, 内存被释放; 值为42的内存的引用计数加1, 变为2;
auto q2 = make_shared<int>(42), r2 = make_shared<int>(100);
r2 = q2;
```